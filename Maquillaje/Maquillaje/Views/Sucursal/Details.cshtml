@model IEnumerable<Maquillaje.Entities.Entities.VW_maqu_tbSucursales_VW>

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="card">
    <div class="card-header">
        <h1>Detalles</h1>
    </div>
    <div class="card-body">
        @foreach (var item in Model)
        {
            <div class="row">
                <div class="form-group col-6">
                    <h5>ID</h5>
                    @Html.DisplayFor(modelItem => item.sucu_Id)
                </div>

                <div class="form-group col-6">
                    <h5>Sucursal</h5>
                    @Html.DisplayFor(modelItem => item.sucu_Descripcion)
                </div>

                <div class="form-group col-6 mt-3">
                    <h5>Municipio</h5>
                    @Html.DisplayFor(modelItem => item.muni_Nombre)
                </div>

                <div class="form-group col-6 mt-3">
                    <h5>Direccion Exacta</h5>
                    @Html.DisplayFor(modelItem => item.sucu_DireccionExacta)
                </div>

            </div>
            <div class="form-group col-3 mt-3">
                <button type="button" id="btnEditar" class="btn btn-warning" onclick="AbrirModalEdit('@item.sucu_Id,@item.sucu_Descripcion,@item.muni_Id,@item.sucu_DireccionExacta,@item.depa_Id')">Editar</button> 
            </div>
        }
        <div style="height:20px"></div>
        <div class="card">
            <div class="card-header"><h2>Auditoria</h2></div>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Accion</th>
                        <th>Usuario</th>
                        <th>Fecha</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td><label>Creacion</label></td>
                            <td>@Html.DisplayFor(modelItem => item.sucu_UsuCreacion_Nombre)</td>
                            <td>@Html.DisplayFor(modelItem => item.sucu_FechaCreacion)</td>
                        </tr>
                        <tr>
                            <td>Modificacion</td>
                            <td>@Html.DisplayFor(modelItem => item.sucu_UsuModificacion_Nombre)</td>
                            <td>@Html.DisplayFor(modelItem => item.sucu_FechaModificacion)</td>
                        </tr>

                    }
                </tbody>
            </table>
        </div>
    </div>
    <a asp-action="Index" class="btn btn-dark">Regresar</a>

</div>

@Html.Partial("_Modal_Edit", new Maquillaje.Entities.Entities.VW_maqu_tbSucursales_VW())

